// card functions
type heroAbility = (roll: number) => void;
type passiveAbility = [() => void, () => void]; // [requirement comparer, effect]
type effect = () => void; // magic and

// card functions
export const cardFunctions = {};
